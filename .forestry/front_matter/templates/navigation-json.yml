---
label: navigation.json
hide_body: true
fields:
- type: field_group_list
  name: Navigation
  label: Navigation Links
  fields:
  - type: text
    name: text
    label: Link Name
    description: The text used for the link.
  - type: boolean
    name: external
    label: Is the link external?
    description: Whether the link goes to somewhere not on this site.
    default: false
  - type: text
    name: url
    label: Link URL
    description: 'The url of the link. If the link is internal, then the Link URL
      <strong>does not</strong> need the beginning of the URL (<a href="https://knowledgylab.com"
      title="https://knowledgylab.com" target="_blank">https://knowledgylab.com</a>).
      The URL only needs the part after the .com, starting with the “/”.<br><br>Internal
      example: "/posts"<br>External example: "https://google.com"<br>If the link is
      external, make sure to <strong>include "</strong>https://" at the beginning!'
    showOnly:
      value: true
    config:
      required: false
  - type: file
    name: icon
    label: Icon
    description: The icon for the Link.
pages:
- site/_data/navigation.json
